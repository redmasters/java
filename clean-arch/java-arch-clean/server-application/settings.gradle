pluginManagement {
    includeBuild('../build-logic')
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

includeBuild('../plataformas')
includeBuild('../usecase')

rootProject.name = 'server-application'
include('app')