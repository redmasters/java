plugins {
    id('java')
}

group = 'io.red.user'

java {
    sourceCompatibility = JavaVersion.VERSION_17
    targetCompatibility = JavaVersion.VERSION_17
}

dependencies {
    implementation(platform('io.red.plataformas:plataforma-produto'))

    testImplementation('org.junit.jupiter:junit-jupiter')
}

tasks.named("test") {
    useJUnitPlatform()
}