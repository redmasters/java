pluginManagement {
    includeBuild('../build-logic')
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

includeBuild('../plataformas')

rootProject.name = 'domain-model'
include('user')
